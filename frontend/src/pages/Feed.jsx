export default function Feed() {
  return (
    <div>
      <h2>Feed</h2>
      <p>
        Add a GET /api/posts route on the server to list posts and render them
        here.
      </p>
    </div>
  );
}
